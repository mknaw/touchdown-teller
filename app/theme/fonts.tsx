import { Poppins } from 'next/font/google';

export const poppins = Poppins({
  subsets: ['latin'],
  weight: '200',
  display: 'swap',
});

export const poppins_400 = Poppins({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
});
